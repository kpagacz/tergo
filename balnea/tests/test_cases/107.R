return(list(
  brush = reactive({
    TRUE
  }),
  click = reactive({
    TRUE
  }),
  dblclick = reactive({
    TRUE
  }),
  hover = reactive({
    TRUE
  }),
  dim = reactive(c(p_width(), p_height())), plot = plot_upd_r
))
